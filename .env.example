# Скопируйте в `.env.local` и при необходимости поправьте.
# Vite отдаёт в код только переменные с префиксом VITE_ (аналог REACT_APP_ в CRA).

VITE_GOOGLE_CLIENT_ID=1082367956142-ntu3usf4p07jpd1enjn7gj308a95qn4v.apps.googleusercontent.com

# Если в Google Cloud Console указан не `window.location.origin`, задайте точный URI:
# VITE_GOOGLE_REDIRECT_URI=https://unet.kstu.kg
